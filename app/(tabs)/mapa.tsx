import { Text, View } from "react-native";
import NavBar from "../navBar";
const Mapa = () => {
    return (
        <View className="flex-1 bg-white">
            <Text>Mapa</Text>
            <NavBar />
        </View>
    )
}

export default Mapa;